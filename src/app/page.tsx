export default function Home() {
  return (
    <div className="min-h-screen bg-[var(--bg-primary)] text-[var(--text-primary)]">
      <main className="relative">
        {/* Pusta sekcja - tutaj będą komponenty */}
      </main>
    </div>
  );
}
